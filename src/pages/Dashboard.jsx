import React from 'react';

const Dashboard = () => {
  // The Dashboard component content has been removed as requested.
  // It is replaced with a simple message to indicate this view is currently unused or disabled.

  return (
    <div className="p-6 bg-gray-100 min-h-screen flex items-center justify-center">
      <div className="bg-white p-10 rounded-xl shadow-lg text-center">
        <h1 className="text-3xl font-bold text-gray-800 mb-4">Dashboard View Removed</h1>
        <p className="text-gray-600">This view is currently empty. Please navigate using the sidebar.</p>
      </div>
    </div>
  );
};

export default Dashboard;
